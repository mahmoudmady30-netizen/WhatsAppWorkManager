org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# Do NOT put any AI provider API keys here for release builds.
# Local-only dev keys may be placed in local.properties (gitignored), never committed.
